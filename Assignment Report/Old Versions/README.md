# Old report versions

This folder stores superseded Word deliverables from `Assignment Report/`.

Archived reports must include `_old_version_YYYY-MM-DD` before the `.docx` extension. The parent folder must contain only one current Word deliverable for each of the group report and four individual reports.
